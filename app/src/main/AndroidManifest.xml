<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tool="http://schemas.android.com/tools"
    package="br.com.beer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tool:ignore="AllowBackup">
        <activity android:name=".ui.activity.BeerConnectionActivity" />
        <activity android:name=".ui.activity.BeerListActivity" />
        <activity
            android:name=".ui.activity.BeerListFavoriteActivity"
            android:parentActivityName=".ui.activity.BeerListActivity" />
        <activity android:name=".ui.activity.BeerSplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.BeerDetailsActivity"
            android:parentActivityName=".ui.activity.BeerListActivity" />
    </application>

</manifest>